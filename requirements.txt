# Runtime dependencies for the Streamlit demo.
# The package itself is defined in pyproject.toml — install with `pip install -e .`
# for development. This file exists because Streamlit Community Cloud reads it.
pandas>=2.0
numpy>=1.24
statsmodels>=0.14
plotly>=5.20
streamlit>=1.40
